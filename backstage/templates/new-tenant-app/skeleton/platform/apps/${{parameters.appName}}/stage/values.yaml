replicaCount: 2

image:
  repository: ${{ parameters.imageRepo }}${{ parameters.appName }}
  tag: "1.0.0-rc1"

ingress:
  enabled: true
  hosts:
    - host: ${{ parameters.appName }}-stage.example.com
      paths:
        - path: /
          pathType: Prefix

env:
  - name: ENVIRONMENT
    value: "stage"
  - name: PORT
    value: "${{ parameters.port }}"

externalSecret:
  enabled: true
  data:
    - secretKey: db-password
      remoteRef:
        key: stage/${{ parameters.appName }}/db-password
