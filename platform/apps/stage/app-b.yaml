apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-b-stage
  namespace: argocd
  labels:
    app: app-b
    environment: stage
spec:
  project: tenant-apps
  source:
    repoURL: https://github.com/tukue/simpleAppInfraCode.git
    targetRevision: main
    path: standardized-path/app
    helm:
      valueFiles:
        - ../../platform/apps/app-b/stage/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: stage
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
